<div class="px-4 w-mid">
  <h1 class="text-3xl font-semibold">Edit Image</h1>

  <%= render "form", image: @image %>
  <%= link_to "BACK" , "/categories/#{@category.id}", class: "mt-4 flex items-center justify-center bg-gray-200 w-full p-1 rounded" %>
</div>