<%= form_with url: "/comments/#{image.id}", method: :post do |form| %>
  <%= form.text_area :query, placeholder: "Some respecful comment here", class: "w-full rounded-md"%>
<!--textarea {
  resize: none;
}-->
  <%= form.submit "COMMENT", class: "bg-gray-500 p-1 px-2 self-start rounded-md  text-white w-full" %>
<% end %>